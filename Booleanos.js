//Booleanos

const isUserOnMajority = true; 

//is, does, has

const disabled = true;
const isDisabled = true;

const userHasGroup = true

// --------------

if (isUserOnMajority) {
    console.log("El usuario es mayor de edad");
} else {
    console.log("El usuario es menor de edad");
}

// Booleanos sempre como perguntas / semântico

loading
isLoading

// Nomenclatura

loading
isLoading

isUserProfileLoading

// loading, disabled, data, fetching, list  //--- Evitar nomes genéricos
// booleanos como perguntas

// if loading, if is user profile loading