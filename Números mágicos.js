// Números mágicos = cálculos difíceis de entender

// Simples

//SetTimeout, setInterval

const INTERVAL_30_DAYS = 1000 * 60 * 60 * 24 * 30;

setTimeout(() => {


}, INTERVAL_30_DAYS); // 30 dias

// Avançado

// Números que simbolizam algo maior - horários e preços

function  calculateDiscount(priceInCents, discountAmountInPercentage) {
    // RETORNA DESCONTO
}